 "use client";
import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";
type P={id:string;name:string;price:number;sale_price:number|null;stock:number;image_url:string|null;description:string|null};
export default function Products(){const[p,setP]=useState<P[]>([]);const[q,setQ]=useState("");const[loading,setLoading]=useState(true);
useEffect(()=>{supabase.from("products").select("id,name,price,sale_price,stock,image_url,description").eq("active",true).order("created_at",{ascending:false}).then(({data})=>{setP(data??[]);setLoading(false)})},[]);
function add(x:P){const c=JSON.parse(localStorage.getItem("one_store_cart")||"[]");const f=c.find((i:any)=>i.id===x.id);if(f)f.quantity++;else c.push({...x,quantity:1});localStorage.setItem("one_store_cart",JSON.stringify(c));alert("تمت الإضافة للسلة")}
const list=p.filter(x=>x.name.toLowerCase().includes(q.toLowerCase()));return <div className="container" style={{padding:"45px 20px",minHeight:600}}><h1>منتجات One Store</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن منتج..." style={{width:"100%",padding:15,borderRadius:12,border:"1px solid #ddd",margin:"15px 0 25px"}}/>{loading?<p>جاري التحميل...</p>:<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:18}}>{list.map(x=><div className="card" key={x.id}><div style={{height:200,display:"grid",placeItems:"center"}}>{x.image_url?<img src={x.image_url} alt={x.name} style={{maxWidth:"100%",maxHeight:200,objectFit:"contain"}}/>:"🛍️"}</div><div style={{padding:18}}><h3>{x.name}</h3><p>{x.description}</p><b style={{color:"#ff7a00"}}>{x.sale_price??x.price} جنيه</b><button className="btn" onClick={()=>add(x)} disabled={!x.stock} style={{width:"100%",marginTop:15}}>أضف للسلة</button></div></div>)}</div>}</div>}
